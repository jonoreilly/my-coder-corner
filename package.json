{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.4"
  }
}
